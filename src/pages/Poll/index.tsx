import Header from '../../components/Header';
import PollView from '../../components/PollView';

const PollPage = () => {
  return (
    <section>
      <Header />
      <PollView />
    </section>
  );
};

export default PollPage;
